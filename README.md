# Recipe Portal Project

Проєкт реалізований згідно з технічним завданням. Використовує Django, Docker, Nginx та ШІ модерацію.

## Як запустити:
1. Клонувати репозиторій.
2. Створити файл `.env` на основі `.env.example`.
3. Виконати команду:
   ```bash
   docker compose up --build